# Система кэширования книг Stefa Books

## Обзор

Система кэширования книг обеспечивает быструю загрузку данных для пользователей и эффективное управление через админ-панель. Книги загружаются один раз из базы данных и сохраняются в локальном кэше браузера.

## Архитектура

### 1. Redux Store (`src/store/books.ts`)
- Управляет кэшем книг в localStorage
- Автоматическая синхронизация с сервером
- Отслеживание изменений через хеши данных
- Версионирование кэша

### 2. API Endpoint (`src/app/api/books/hash/route.ts`)
- Предоставляет хеш данных для проверки изменений
- Позволяет определить, нужна ли синхронизация

### 3. Хук (`src/hooks/useBooksCache.ts`)
- Удобный интерфейс для работы с кэшем
- Автоматическая инициализация
- Фоновая проверка обновлений

### 4. Компонент статуса (`src/app/admin/components/CacheStatus.tsx`)
- Отображает статус кэша в админ-панели
- Управление синхронизацией
- Мониторинг состояния

## Как это работает

### Для пользователей:
1. **Первый визит**: Книги загружаются с сервера и сохраняются в кэш
2. **Последующие визиты**: Книги загружаются мгновенно из кэша
3. **Автоматическое обновление**: Кэш синхронизируется каждые 30 минут
4. **Офлайн доступ**: Работает без интернета

### Для админов:
1. **Мониторинг**: Видят статус кэша в реальном времени
2. **Принудительная синхронизация**: Могут обновить кэш вручную
3. **Уведомления**: Получают уведомления о необходимости синхронизации
4. **Статистика**: Видят количество книг и время последней синхронизации

## Преимущества

- **Экономия трафика**: Книги загружаются один раз
- **Быстрая работа**: Нет задержек на загрузку
- **Автоматизация**: Кэш обновляется автоматически
- **Мониторинг**: Полный контроль через админ-панель
- **Офлайн доступ**: Работает без интернета

## Использование

### В компонентах:
```typescript
import { useBooksCache } from '@/hooks/useBooksCache'

function MyComponent() {
  const { 
    books, 
    getFilteredBooks, 
    isSyncing 
  } = useBooksCache()
  
  const availableBooks = getFilteredBooks({ availableOnly: true })
  
  return (
    <div>
      {isSyncing ? 'Синхронизация...' : `Книг: ${books.length}`}
    </div>
  )
}
```

### Принудительная синхронизация:
```typescript
const { forceSync } = useBooksCache()

// Обновить кэш вручную
await forceSync()
```

### Фильтрация книг:
```typescript
const { getFilteredBooks } = useBooksCache()

// По категории
const categoryBooks = getFilteredBooks({ categoryId: 'fiction' })

// Поиск
const searchResults = getFilteredBooks({ search: 'фантастика' })

// Доступные книги
const availableBooks = getFilteredBooks({ availableOnly: true })

// С лимитом
const limitedBooks = getFilteredBooks({ limit: 10 })
```

## Настройка

### Время синхронизации:
По умолчанию кэш синхронизируется каждые 30 минут. Изменить можно в `useBooksCache.ts`:

```typescript
const syncInterval = 30 * 60 * 1000 // 30 минут
```

### Проверка обновлений:
Проверка обновлений происходит каждые 5 минут. Изменить можно в `CacheStatus.tsx`:

```typescript
const interval = setInterval(checkUpdates, 5 * 60 * 1000) // 5 минут
```

## Мониторинг

### В консоли браузера:
- ✅ Cache hit - данные взяты из кэша
- ❌ Cache miss - кэш не найден
- ⏰ Cache expired - кэш устарел
- 💾 Cache set - данные сохранены в кэш
- 🔄 Data changed, sync needed - нужна синхронизация

### В админ-панели:
- Статус кэша (Актуально/Синхронизация/Есть обновления)
- Количество книг в кэше
- Время последней синхронизации
- Версия кэша
- Уведомления об изменениях

## Устранение неполадок

### Кэш не загружается:
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что API endpoint `/api/books/hash` работает
3. Проверьте localStorage на наличие данных

### Синхронизация не работает:
1. Проверьте сетевые запросы в DevTools
2. Убедитесь, что API endpoint `/api/books` доступен
3. Проверьте права доступа к базе данных

### Производительность:
1. Мониторьте размер localStorage
2. Проверьте время выполнения фильтрации
3. Оптимизируйте количество книг в кэше

## Будущие улучшения

- [ ] Инкрементальная синхронизация
- [ ] Сжатие данных в localStorage
- [ ] Умная очистка устаревших данных
- [ ] Аналитика использования кэша
- [ ] Настройки кэширования для разных типов данных
